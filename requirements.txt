# The 0.10.14 release exposes the classic `mp.solutions.pose` API used by the
# Stage 1 adapter. It supports Python 3.10 through 3.11; the launchers select a
# compatible installed Python version automatically.
mediapipe==0.10.14
opencv-python>=4.9.0,<5.0
numpy>=1.26.0,<2.0
PySide6>=6.6.0,<6.11
pytest>=8.0.0,<9.0
pyinstaller>=6.0,<7.0
