# The 0.10.14 release exposes the classic `mp.solutions.pose` API used by the
# Stage 1 adapter. Later distributions may expose only the Tasks API.
mediapipe==0.10.14
opencv-python>=4.9.0,<5.0
numpy>=1.26.0,<2.0
PySide6>=6.6.0,<6.11
pytest>=8.0.0,<9.0
pyinstaller>=6.0,<7.0
