# Application icons

`app.ico` and `app.icns` are generated automatically from the Workout Tracker
icon design when the Windows or macOS build script runs. Replace the icon
generation in `scripts/build_support.py` or provide replacement files with
these exact names before building.
