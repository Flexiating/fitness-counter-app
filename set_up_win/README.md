# Windows setup

Double-click `install_python_312.bat` before the first launch if Python 3.12 is
not installed. It uses Windows Package Manager when available, otherwise it
opens the official Python download page.

After Python finishes installing, close and reopen Command Prompt, then
double-click `run_windows.bat` in the main project folder.
